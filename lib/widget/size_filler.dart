import 'package:flutter/material.dart';

class SizeFiller extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        height: 0,
      ),
    );
  }
}